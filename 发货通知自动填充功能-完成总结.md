# 发货通知自动填充功能 - 完成总结

## 📋 需求回顾

**用户需求**：
> "新增发货通知时，订单信息和详情信息应该都查找出来，填充发货详情，卷数可以修改，这个功能才完善"

## ✅ 完成内容

### 1. 前端代码优化

**文件位置**：[src/views/sales/delivery.vue](src/views/sales/delivery.vue)

**修改内容**：
- 优化 `fetchOrderDetails()` 方法
- 从调用列表接口改为调用详情接口 `GET /sales/orders/{orderNo}`
- 增强订单明细映射逻辑

**关键改进**：

```javascript
async fetchOrderDetails() {
  if (!this.currentNotice.orderNo) return
  
  try {
    // 调用订单详情接口，直接获取订单及其明细信息
    const res = await request({
      url: `/sales/orders/${this.currentNotice.orderNo}`,
      method: 'get'
    })

    if (res.code === 200 && res.data) {
      const order = res.data
      
      // 填充订单基本信息
      this.currentNotice.orderId = order.id
      this.currentNotice.customer = order.customer
      this.currentNotice.deliveryAddress = order.deliveryAddress
      this.currentNotice.contactPerson = order.contactPerson
      this.currentNotice.contactPhone = order.contactPhone
      this.currentNotice.customerOrderNo = order.customerOrderNo || ''
      
      // 填充发货明细（将订单明细映射到发货明细）
      if (order.items && order.items.length > 0) {
        this.currentNotice.items = order.items.map(oItem => {
          // 计算未发货数量（订单数量 - 已发货数量）
          const remainingRolls = (oItem.rolls || 0) - (oItem.shippedRolls || 0)
          
          return {
            orderItemId: oItem.id,
            materialCode: oItem.materialCode,
            materialName: oItem.materialName,
            spec: `${oItem.width || ''}*${oItem.thickness || ''}*${oItem.length || ''}`,
            quantity: remainingRolls > 0 ? remainingRolls : 0, // 默认填充未发货数量，用户可修改
            areaSize: oItem.areaSize || 0,
            boxCount: 0,
            grossWeight: 0,
            totalWeight: 0,
            remark: `订单数量：${oItem.rolls}卷，已发货：${oItem.shippedRolls || 0}卷`
          }
        })
        
        this.$message.success('已加载订单信息和明细')
      } else {
        this.$message.warning('该订单没有明细数据')
      }
    } else {
      this.$message.warning('未找到该销售订单')
    }
  } catch (e) {
    console.error('加载订单详情失败：', e)
    this.$message.error('加载订单详情失败，请检查订单号是否正确')
  }
}
```

### 2. 后端接口确认

**Controller**：[SalesOrderController.java](../../java/MES/src/main/java/com/fine/controller/SalesOrderController.java)

**接口端点**：
```java
@GetMapping("/{orderNo}")
public ResponseResult<?> getOrderDetail(@PathVariable String orderNo) {
    System.out.println("=== 获取订单详情 ===");
    System.out.println("订单号: " + orderNo);
    return salesOrderService.getOrderByOrderNo(orderNo);
}
```

**Service实现**：[SalesOrderServiceImpl.java](../../java/MES/src/main/java/com/fine/serviceIMPL/SalesOrderServiceImpl.java)

```java
public ResponseResult<?> getOrderByOrderNo(String orderNo) {
    LambdaQueryWrapper<SalesOrder> queryWrapper = new LambdaQueryWrapper<>();
    queryWrapper.eq(SalesOrder::getOrderNo, orderNo);
    SalesOrder order = salesOrderMapper.selectOne(queryWrapper);
    
    if (order != null) {
        // 获取订单详情时，加载明细数据
        LambdaQueryWrapper<SalesOrderItem> itemWrapper = new LambdaQueryWrapper<>();
        itemWrapper.eq(SalesOrderItem::getOrderId, order.getId())
                  .eq(SalesOrderItem::getIsDeleted, 0);
        List<SalesOrderItem> items = salesOrderItemMapper.selectList(itemWrapper);
        
        // 填充颜色代码等信息
        enrichItemsWithSpecInfo(items);
        
        // 填充已发货数量
        for (SalesOrderItem item : items) {
            Integer shipped = deliveryNoticeItemMapper.getShippedQuantityByOrderItemId(item.getId());
            item.setShippedRolls(shipped);
        }

        order.setItems(items);
    }
    
    return new ResponseResult<>(200, "success", order);
}
```

## 🎯 功能特性

### ✨ 自动填充字段

#### 订单基本信息
- ✅ 订单ID（orderId）
- ✅ 客户名称（customer）
- ✅ 收货地址（deliveryAddress）
- ✅ 联系人（contactPerson）
- ✅ 联系电话（contactPhone）
- ✅ 客户订单号（customerOrderNo）

#### 发货明细
- ✅ 物料代码（materialCode）
- ✅ 物料名称（materialName）
- ✅ 规格（spec）：宽*厚*长
- ✅ 发货数量（quantity）：**智能计算未发货数量** = 订单数量 - 已发货数量
- ✅ 面积（areaSize）
- ✅ 备注（remark）：显示订单和已发货信息

### 🔧 用户可编辑字段

用户在自动填充后，可以修改以下字段：

1. **发货数量（quantity）**：使用 `el-input-number` 组件，可以通过输入框或加减按钮调整
2. **箱数（boxCount）**
3. **每箱毛重（grossWeight）**
4. **总毛重（totalWeight）**
5. **备注（remark）**

### 🔒 锁定字段

以下字段从订单带入，不可修改（编辑模式）：

- ❌ 物料代码（materialCode）
- ❌ 物料名称（materialName）
- ❌ 规格（spec）

### 💡 智能特性

1. **防止重复发货**：
   - 自动计算未发货数量（订单数量 - 已发货数量）
   - 默认填充未发货数量，避免超发

2. **备注信息追踪**：
   - 自动在备注中显示订单数量和已发货数量
   - 例如："订单数量：100卷，已发货：50卷"

3. **用户体验优化**：
   - 失去焦点即触发（`@blur`事件）
   - 成功提示："已加载订单信息和明细"
   - 错误提示："未找到该销售订单"
   - 空明细警告："该订单没有明细数据"

## 📊 测试数据

### 可用测试订单

| 订单号 | 客户 | 明细行数 | 备注 |
|--------|------|---------|------|
| SO-202412-001 | 深圳华为技术有限公司 | 2 | 部分发货（MT-001已发50卷） |
| SO-202412-002 | 东莞比亚迪汽车有限公司 | 2 | 未发货 |
| SO-202412-003 | 广州小鹏汽车科技有限公司 | 3 | 未发货 |

### SO-202412-001 示例数据

**订单信息**：
- 客户：深圳华为技术有限公司
- 地址：深圳市龙岗区坂田华为基地
- 联系人：张经理
- 电话：0755-12345678

**发货明细**：
| 物料代码 | 物料名称 | 规格 | 订单数量 | 已发货 | 未发货（自动填充） |
|---------|---------|------|---------|--------|------------------|
| MT-001 | 3M胶带 | 48*0.15*50 | 100 | 50 | **50** ⬅ 可修改 |
| MT-002 | 泡棉胶带 | 24*0.08*50 | 200 | 0 | **200** ⬅ 可修改 |

## 🚀 使用流程

### 操作步骤

1. **打开创建发货通知对话框**
   - 点击"新增发货通知"按钮

2. **输入订单号**
   - 在"销售订单"输入框输入：`SO-202412-001`

3. **触发自动填充**
   - 按 `Tab` 键或点击其他输入框（失去焦点）
   - 系统自动查询并填充订单信息

4. **确认并调整**
   - 检查自动填充的客户信息和发货明细
   - 根据实际情况修改发货数量

5. **完善其他信息**
   - 选择发货日期
   - 填写承运公司、运输电话等

6. **保存**
   - 点击"保存"按钮
   - 系统提示"保存成功"

### 触发条件

- ✅ 订单号输入框失去焦点（`@blur` 事件）
- ✅ 订单号不为空
- ✅ 非编辑模式（`:disabled="isEdit"`）

## 📁 相关文档

1. [发货通知自动填充功能说明.md](发货通知自动填充功能说明.md) - 功能详细说明
2. [发货通知功能测试指南.md](发货通知功能测试指南.md) - 完整测试步骤和用例

## 🔍 技术细节

### 前端触发机制

```vue
<el-form-item label="销售订单" prop="orderNo">
  <el-input 
    v-model="currentNotice.orderNo" 
    placeholder="请输入关联订单号" 
    @blur="fetchOrderDetails"    ⬅ 失去焦点时触发
    :disabled="isEdit" 
  />
</el-form-item>
```

### 数量输入组件

```vue
<el-table-column label="发货数量(卷)" width="140">
  <template slot-scope="scope">
    <el-input-number 
      v-model="scope.row.quantity" 
      :min="0" 
      size="small" 
      style="width: 100px;" 
    />
  </template>
</el-table-column>
```

### 数据映射逻辑

```javascript
// 订单明细 → 发货明细
{
  orderItemId: oItem.id,              // 关联订单明细ID
  materialCode: oItem.materialCode,   // 物料代码
  materialName: oItem.materialName,   // 物料名称
  spec: `${width}*${thickness}*${length}`,  // 规格拼接
  quantity: remainingRolls,           // 未发货数量
  areaSize: oItem.areaSize || 0,      // 面积
  remark: `订单数量：${rolls}卷，已发货：${shipped}卷`  // 追踪信息
}
```

## ⚠️ 注意事项

1. **权限要求**：需要 `admin` 或 `sales` 角色才能访问销售订单接口
2. **网络依赖**：需要后端服务正常运行在 `http://localhost:8090`
3. **数据完整性**：确保销售订单数据包含完整的明细信息
4. **编辑限制**：编辑模式下订单号字段被禁用，不能切换订单

## 🎉 完成状态

- ✅ 前端代码优化完成
- ✅ 后端接口验证完成
- ✅ 自动填充功能实现
- ✅ 数量可修改功能实现
- ✅ 智能计算未发货数量
- ✅ 完整文档编写
- ✅ 测试指南创建

## 📝 后续优化建议

1. **库存检查**：发货前检查库存是否充足
2. **批次管理**：支持选择具体的库存批次进行发货
3. **自动计算重量**：根据物料规格和数量自动计算毛重
4. **发货单号规则**：自动生成发货单号（例如：DN-202412-001）
5. **发货限制**：增加发货数量不能超过未发货数量的校验

---

**完成时间**：2026-01-14  
**修改文件**：
- 前端：`e:\vue\ERP\src\views\sales\delivery.vue`
- 后端：已有接口，无需修改

**测试状态**：✅ 代码已完成，等待用户测试验证
