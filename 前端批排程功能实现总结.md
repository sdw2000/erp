# ✅ 前端批排程功能 - 实现总结

## 📋 已完成任务

### 1. 表格列优化

| 列名 | 改进内容 | 文件位置 |
|------|--------|--------|
| **待排数量** | 改用 el-tag 标签，根据数量显示颜色（待排>0:橙黄，待排=0:绿色） | L119-128 |
| **本次排程数** | 改用 el-input type="number"，替代 el-input-number | L129-140 |

### 2. 验证逻辑强化

```javascript
// 位置: L1465-1486
validateScheduleQty(row) {
  // ✅ 处理空值和非数字
  if (!row.schedule_qty || row.schedule_qty === '') {
    row.schedule_qty = 0
    return
  }
  
  // ✅ 转换为数字
  row.schedule_qty = Number(row.schedule_qty)
  
  // ✅ 不能为负数
  if (row.schedule_qty < 0) {
    row.schedule_qty = 0
    this.$message.warning('排程数量不能为负数')
  }
  
  // ✅ 不能超过待排数量
  if (row.schedule_qty > row.pending_qty) {
    row.schedule_qty = row.pending_qty
    this.$message.warning(`排程数量不能超过待排数量 ${row.pending_qty}`)
  }
  
  // ✅ 整数处理
  row.schedule_qty = Math.floor(row.schedule_qty)
}
```

### 3. 生命周期优化

```javascript
// 位置: L1117-1119
mounted() {
  this.loadScheduleList()
  this.loadPendingOrders()  // ← 新增：组件加载时自动加载待排程订单
}
```

### 4. 数据初始化

```javascript
// 位置: L1185-1193
async loadPendingOrders() {
  // ...
  this.pendingOrders.forEach(order => {
    if (!order.schedule_qty) {
      order.schedule_qty = order.pending_qty  // ← 默认值 = 待排数量
    }
  })
}
```

### 5. 批排程逻辑

```javascript
// 位置: L1282-1330
batchSchedule() {
  // ✅ 检查选中的订单和排程数量
  const validOrders = this.selectedOrders.filter(o => o.schedule_qty > 0)
  
  // ✅ 准备排程数据
  const scheduleDetails = validOrders.map(o => ({
    order_item_id: o.order_item_id,
    schedule_qty: o.schedule_qty              // ← 使用用户输入的值
  }))
  
  // ✅ 调用 API
  const res = await autoSchedule({
    scheduleDate: this.formatDate(new Date()),
    scheduleType: 'order',
    details: scheduleDetails,                  // ← 新参数结构
    operator: 'admin'
  })
}
```

---

## 🎯 核心改进点

### A. 输入体验改进

**前**：el-input-number（带加减号）
```
  -  [100]  +
```
❌ 加减号遮挡数字，数据不清晰

**后**：el-input type="number"
```
[     100     ]
```
✅ 清晰展示，加粗字体，易于识别

---

### B. 状态反馈改进

**前**：纯文字显示
```
待排数量: 100
```

**后**：彩色标签显示
```
🟠 100  (待排>0，警告色)
🟢  0   (待排=0，成功色)
```

---

### C. 验证逻辑改进

**前**：基础验证（只检查边界）
```javascript
if (schedule_qty > pending_qty) {
  schedule_qty = pending_qty
}
```

**后**：完整验证（多层检查）
```
1. 空值检查 → 设为 0
2. 类型转换 → Number()
3. 负数检查 → 显示警告
4. 范围检查 → 不超过 pending_qty
5. 整数处理 → Math.floor()
```

---

### D. 数据流优化

```
【用户打开页面】
     ↓
【mounted 触发】
     ├─ loadScheduleList()      (加载排程列表)
     └─ loadPendingOrders()     (加载待排程订单) ← 新增
     ↓
【显示两个 tab 的数据】
     ├─ 计划管理 tab (排程历史)
     └─ 待排程订单 tab (可排程订单)
```

---

## 📊 关键数据字段

### sales_order_items 表字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | INT | 明细ID | 1 |
| `order_item_id` | INT | 关联订单明细 | 1 |
| `material_code` | VARCHAR | 料号 | TAPE-001 |
| `item_qty` | INT | 明细订单数量 | 100 |
| `pending_qty` | INT | ⭐ 待排数量 | 40 |
| `produced_qty` | INT | 已生产数量 | 0 |
| `order_qty` | INT | 订单总数量 | 100 |

### 前端行对象结构

```javascript
row = {
  id: 1,
  order_no: "PO-001",
  order_item_id: 1,      // ← 用于后端 API 参数
  customer: "ABC公司",
  customer_level: "VIP",
  material_code: "TAPE-001",
  material_name: "透明胶带",
  color_code: "蓝",
  thickness: 0.05,
  width: 100,
  length: 5000,
  order_qty: 100,        // ← 原始订单数
  pending_qty: 40,       // ← 显示在表格中（彩色标签）
  produced_qty: 0,       // ← 已生产数
  schedule_qty: 40,      // ← 用户输入的本次排程数
  delivery_date: "2026-01-15",
  // ... 其他字段
}
```

---

## 🔄 前后端交互

### API 调用

**前端发送**：
```javascript
autoSchedule({
  scheduleDate: "2026-01-10",
  scheduleType: "order",
  details: [
    { order_item_id: 1, schedule_qty: 60 },
    { order_item_id: 2, schedule_qty: 50 }
  ],
  operator: "admin"
})
```

**后端需要处理**：
```
1. 验证 schedule_qty ≤ pending_qty
2. 创建 schedule_order_item 记录
3. 更新 sales_order_items.pending_qty -= schedule_qty
4. 创建工序任务
5. 返回成功/失败
```

---

## ✨ 代码位置快速导航

| 功能 | 文件 | 行号 |
|------|------|------|
| 表格定义 | schedule.vue | L5-165 |
| 待排数量列 | schedule.vue | L119-128 |
| 排程数列 | schedule.vue | L129-140 |
| 验证方法 | schedule.vue | L1465-1486 |
| 加载方法 | schedule.vue | L1180-1200 |
| 排程方法 | schedule.vue | L1282-1330 |
| mounted 钩子 | schedule.vue | L1117-1119 |

---

## 🎬 演示脚本

### 场景：批排程两个订单

```
【1】页面加载
  → mounted() 触发
  → 自动加载待排程订单
  → 显示 pending_qty > 0 的订单

【2】用户勾选订单
  ✓ PO-001 (pending_qty=100)
  ✓ PO-002 (pending_qty=85)

【3】用户输入排程数
  PO-001: 输入 150 → 验证失败 → 自动改为 100
  PO-002: 输入 85   → 验证通过 ✓

【4】点击"批量排程(2)"
  → 显示确认框: "确认排程 2 个订单，总计 185 卷？"

【5】用户点击"确定"
  → 前端调用 autoSchedule API
  → 传递 details: [{id:1, qty:100}, {id:2, qty:85}]

【6】后端处理
  → 验证参数
  → 创建排程记录
  → 更新 pending_qty
  → 创建工序任务

【7】API 返回成功
  → 前端显示成功提示
  → 自动刷新两个列表

【8】用户看到新的状态
  → PO-001: pending_qty 100 → 0 (完全排程)
  → PO-002: pending_qty 85 → 0 (完全排程)
```

---

## 📝 前端实现检查清单

- ✅ 表格列：待排数量（彩色标签）
- ✅ 表格列：本次排程数（纯输入框）
- ✅ 验证方法：validateScheduleQty()
- ✅ 初始化：loadPendingOrders() 中设置 schedule_qty
- ✅ 生命周期：mounted() 自动加载
- ✅ 批排程：batchSchedule() 收集数据
- ✅ 数据格式：details[] 参数结构
- ✅ 代码格式：ESLint 检查通过
- ✅ 错误处理：未完成（待后端 API）

---

## 🚀 后续步骤

### 立即需要
1. **后端 API 实现** - 修改 `/api/production/schedule/auto`
   - 接受 `details[]` 参数
   - 验证排程数量
   - 更新 pending_qty
   - 创建排程记录

2. **数据库迁移** - 添加字段（如未存在）
   - `sales_order_items.pending_qty`
   - 创建 `schedule_order_item` 表

### 可选优化
1. 添加排程历史查询功能
2. 导出排程单为 PDF
3. 排程计划与生产看板联动
4. 排程数据分析报表

---

## 📂 相关文档

- `排程关联表设计方案对比.md` - 数据库设计
- `方案A实现总结.md` - 完整实现方案
- `未排程数量快速指南.md` - 快速参考
- `前端排程表格可视化演示.md` - 视觉演示

