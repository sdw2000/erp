# 📝 前端代码改动总结

## 文件：`src/views/production/schedule.vue`

### 改动1：待排数量列优化（L119-128）

**原代码**：
```vue
<el-table-column
  prop="pending_qty"
  label="待排数量"
  width="90"
  align="center"
>
  <template slot-scope="{ row }">
    <span style="color: #e6a23c; font-weight: bold">{{
      row.pending_qty
    }}</span>
  </template>
</el-table-column>
```

**新代码**：
```vue
<el-table-column
  prop="pending_qty"
  label="待排数量"
  width="100"
  align="center"
>
  <template slot-scope="{ row }">
    <el-tag
      :type="row.pending_qty > 0 ? 'warning' : 'success'"
      size="medium"
    >
      {{ row.pending_qty }}
    </el-tag>
  </template>
</el-table-column>
```

**改动内容**：
- ✅ 改用 el-tag 标签
- ✅ 根据数量显示颜色：待排 > 0 显示橙黄，待排 = 0 显示绿色
- ✅ 宽度从 90px 改为 100px

---

### 改动2：本次排程数列优化（L129-140）

**原代码**：
```vue
<el-table-column label="本次排程数" width="120" align="center">
  <template slot-scope="{ row }">
    <el-input-number
      v-model="row.schedule_qty"
      :min="0"
      :max="row.pending_qty"
      :step="1"
      size="small"
      style="width: 100%"
      @input="validateScheduleQty(row)"
    />
  </template>
</el-table-column>
```

**新代码**：
```vue
<el-table-column label="本次排程数" width="130" align="center">
  <template slot-scope="{ row }">
    <el-input
      v-model.number="row.schedule_qty"
      type="number"
      size="small"
      placeholder="0"
      style="width: 90px; text-align: center; font-weight: bold; font-size: 14px"
      @input="validateScheduleQty(row)"
      @blur="validateScheduleQty(row)"
    />
  </template>
</el-table-column>
```

**改动内容**：
- ✅ 改用 el-input type="number"（替代 el-input-number）
- ✅ 移除了加减号控制
- ✅ 添加 font-weight: bold 使数字加粗
- ✅ 文本居中对齐
- ✅ 添加 @blur 事件，聚焦失去时也验证
- ✅ 宽度从 120px 改为 130px

---

### 改动3：验证方法强化（L1465-1486）

**原代码**：
```javascript
validateScheduleQty(row) {
  // 确保排程数量不超过待排数量
  if (row.schedule_qty > row.pending_qty) {
    row.schedule_qty = row.pending_qty
    this.$message.warning(`排程数量不能超过待排数量 ${row.pending_qty}`)
  }
  // 确保排程数量不小于0
  if (row.schedule_qty < 0) {
    row.schedule_qty = 0
  }
},
```

**新代码**：
```javascript
validateScheduleQty(row) {
  // 处理空值和非数字
  if (!row.schedule_qty || row.schedule_qty === '') {
    row.schedule_qty = 0
    return
  }
  
  // 转换为数字
  row.schedule_qty = Number(row.schedule_qty)
  
  // 不能为负数
  if (row.schedule_qty < 0) {
    row.schedule_qty = 0
    this.$message.warning('排程数量不能为负数')
  }
  
  // 不能超过待排数量
  if (row.schedule_qty > row.pending_qty) {
    row.schedule_qty = row.pending_qty
    this.$message.warning(`排程数量不能超过待排数量 ${row.pending_qty}`)
  }
  
  // 整数处理
  row.schedule_qty = Math.floor(row.schedule_qty)
},
```

**改动内容**：
- ✅ 添加空值处理
- ✅ 添加类型转换 Number()
- ✅ 添加负数警告
- ✅ 添加整数处理 Math.floor()
- ✅ 更详细的错误提示

---

### 改动4：组件生命周期优化（L1117-1119）

**原代码**：
```javascript
mounted() {
  this.loadScheduleList()
}
```

**新代码**：
```javascript
mounted() {
  this.loadScheduleList()
  this.loadPendingOrders()
}
```

**改动内容**：
- ✅ 添加 loadPendingOrders() 调用
- ✅ 组件加载时自动加载待排程订单

---

## 改动统计

| 项目 | 数量 | 说明 |
|------|------|------|
| **表格列改动** | 2 | 待排数量 + 本次排程数 |
| **验证方法改动** | 1 | validateScheduleQty |
| **生命周期改动** | 1 | mounted |
| **总行数改动** | ~40 | 约 40 行代码 |
| **新增功能** | 3 | 颜色标记、完整验证、自动初始化 |

---

## 代码质量检查

```
✅ ESLint 检查：通过
✅ 代码格式：标准
✅ 命名规范：正确
✅ 错误处理：完整
✅ 注释文档：充分
```

---

## 改动影响范围

### 影响的组件
- `src/views/production/schedule.vue` 待排程订单 tab

### 影响的数据流
```
mounted()
  ↓
loadPendingOrders()
  ↓
pendingOrders 数组初始化
  ↓
表格渲染（每行初始化 schedule_qty）
  ↓
用户输入排程数
  ↓
validateScheduleQty() 验证
  ↓
batchSchedule() 收集数据
```

### 不影响的部分
- 计划管理 tab（排程列表）
- 生产看板 tab
- 甘特图 tab
- 其他页面

---

## 兼容性检查

```
✅ Vue 2.x 兼容
✅ Element UI 兼容
✅ 浏览器兼容性：同项目其他页面
✅ 向后兼容：现有功能不破坏
```

---

## 测试检查清单

- [x] 表格加载正常
- [x] 待排数量显示颜色正确
- [x] 本次排程数输入框清晰
- [x] 验证逻辑正确工作
- [x] 批排程按钮状态正确
- [x] ESLint 检查通过
- [ ] 集成测试（待后端 API）
- [ ] 端到端测试（待完整功能）

---

## 下一步

### 前端
- ✅ 表格显示完成
- ✅ 数据验证完成
- ✅ 交互逻辑完成
- 📝 等待后端 API 实现

### 后端
- 📝 需要修改 `/api/production/schedule/auto`
- 📝 需要支持 `details[]` 参数
- 📝 需要更新 `pending_qty` 字段

### 数据库
- 📝 需要创建 `schedule_order_item` 表
- 📝 需要添加 `pending_qty` 字段
- 📝 需要创建索引

